name: namae-web
on:
  push:
    paths:
      - web/*
      - .github/**
jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@master
      - uses: actions/checkout@master
      - run: cd web
      - run: yarn install
      - run: yarn test